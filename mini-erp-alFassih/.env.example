# Flask Configuration
FLASK_APP=runserver.py
# FLASK_CONFIG options: development, production, default
FLASK_CONFIG=development

# Application Secrets and Configs
# IMPORTANT: Generate a real, strong random secret key for SECRET_KEY and keep it private.
# You can generate one using: python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY='your_strong_random_secret_key_here_CHANGE_ME'

# Database URL
# For development using SQLite in the 'instance' folder (relative to instance_path):
DATABASE_URL='sqlite:///alfassih.db'
# Example for PostgreSQL:
# DATABASE_URL='postgresql://user:password@host:port/dbname'
# Example for MySQL:
# DATABASE_URL='mysql+pymysql://user:password@host:port/dbname'

# Other application-specific configurations can be added here
# e.g. UPLOAD_FOLDER_NAME='my_uploads' (if you want to override the default in config.py)
```
